Arr = [1, 32, 51, 12, 512, 53, 34]

def sort(arr):
    for i in range(0, len(arr)):
        for j in range(0, len(arr)):
            if arr[i] < arr[j]:
                temp = arr[i]
                arr[i] = arr[j]
                arr[j] = temp

    return arr

print(sort(Arr))
